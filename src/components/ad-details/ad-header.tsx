import { X } from "lucide-react"

interface AdHeaderProps {
  onClose: () => void
}

export function AdHeader({ onClose }: AdHeaderProps) {
  return null // We're moving this component's functionality to the sidebar
}